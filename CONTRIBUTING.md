# 协作与 Git 提交规范（DSH 实验室 001 号）

这份文档约定**这个工作区怎么用 Git**：提交信息写什么、什么时候可以自动提交、
哪些东西永远不入库、以及钩子怎么帮我们兜底。

它只约束「怎么提交」，不约束「实验怎么做」。实验内容、技术选型、目录内的写法由各实验自己的 `README.md` 决定。

- 远程仓库：<https://github.com/andyluss/dsh001>
- 默认分支：`main`
- 首次克隆后执行一次：`git config core.hooksPath .githooks`

---

## 一、一次提交的完整流程

1. `git status` —— 看清工作区状态，确认没有夹带无关改动；
2. `git diff` / `git diff --staged` —— 逐块看一遍要提交的内容；
3. **明确指定路径** `git add <path>...`（不要用 `git add -A` / `git add .` 一把梭）；
4. `git diff --staged --stat` —— 确认暂存内容与预期一致；
5. 需要时先跑实验自检（如实验 001 的 `npm test`）；
6. `git commit` —— 按下面的规范写提交信息（模板已配好，直接填主题行）；
7. `git log --oneline -5` —— 提交后回看一眼。

> 提交是**记录**，不是**保存**。一次提交 = 一件可独立描述、可独立回滚的事。
> 宁可多提交几次，也不要把「重命名 + 改功能 + 格式化」塞进同一个提交。

---

## 二、提交信息规范

采用 **Conventional Commits 的一个子集**，并在其之上叠加中文约定。

### 2.1 格式

```text
<type>(<scope>): <中文描述>

<正文：为什么这么改，以及取舍>

<脚注：Refs / Closes / BREAKING CHANGE>
```

- `type` 必填，取自下面的类型表；
- `scope` 可选，建议写实验编号或模块名；
- 冒号后面**必须有一个空格**，然后是中文描述；
- 空行分隔主题行与正文，正文与脚注之间再空一行。

### 2.2 type 类型表

| type | 用于 | 例子 |
| --- | --- | --- |
| `feat` | 新增对使用者可见的能力 | `feat(001-cards): 新增标签树拖拽调整层级` |
| `fix` | 修复缺陷 | `fix(001-cards): 修复复习队列重复计入已删除卡片` |
| `docs` | 只改文档、注释、规范 | `docs: 补充实验 002 的家族设计索引` |
| `style` | 不影响语义的格式改动（空白、分号、排版） | `style(001-cards): 统一 JSX 缩进为两空格` |
| `refactor` | 重构：既不新增功能也不修缺陷 | `refactor(001-cards): 把树操作从组件抽到 store.js` |
| `perf` | 性能优化 | `perf(001-cards): 搜索改为一次遍历复用索引` |
| `test` | 新增/调整测试 | `test(001-cards): 补充 SM-2 到期计算的边界用例` |
| `build` | 构建系统与依赖 | `build(001-cards): 升级 vite 到 5.4` |
| `ci` | CI 配置与自动化脚本 | `ci: 增加提交信息格式检查` |
| `chore` | 杂务：不属于以上的仓库级改动 | `chore: 初始化实验室仓库与忽略配置` |
| `revert` | 回滚某次提交 | `revert: 回滚「新增拖拽排序」` |

选不准时的判断顺序：**改了文档 → `docs`；改了行为 → `feat`/`fix`；改了结构但行为不变 → `refactor`；
都不是 → `chore`。**

### 2.3 scope 约定

| scope | 含义 |
| --- | --- |
| `001-cards` | 实验 001 · 知识卡片（`lab/001-knowledge-cards/`） |
| `002-liveskin` | 实验 002 · LiveSkin 皮肤系统（`lab/002-liveskin/`） |
| `docs` / `punks` | `doc/` 下的文档与研究卷 |
| `repo` | 仓库级：README、CONTRIBUTING、.gitignore、.githooks |
| `git` | Git 工作流本身（钩子、模板、脚本） |

跨实验或纯仓库级改动可省略 scope，或直接用 `repo`。

### 2.4 中文提交约定（硬性）

1. **主题行必须含中文**，用中文说清「做了什么」。专有名词、命令、文件名、API 名可保留英文；
2. 禁止纯英文、纯 emoji、纯数字、纯符号的提交信息；
3. 禁止无信息量的描述：`update`、`修改`、`修复 bug`、`提交`、`wip`、`临时`、`111`；
4. 用**动词开头**的陈述句，写「做了什么」，不加句号；
   例：`新增…`、`修复…`、`重写…`、`移除…`、`调整…`、`补充…`；
5. 正文同样用中文；引用报错、命令、标识符时保留原文；
6. 主题行控制在 **50 个字符以内**（`commit-msg` 钩子会拦截超长）。

### 2.5 正文与脚注

- 正文写「**为什么**」：动机、约束、被否掉的方案、留待后续的坑；
  「改了什么」diff 里已经有了，不必复述；
- 每行不超过 72 个字符（中文按字符算），便于 `git log` 阅读；
- 关联信息放脚注：

```text
Refs: #12
Closes: #12
BREAKING CHANGE: kcards.v1 结构变更，旧数据需重新导入
```

### 2.6 正例与反例

✅ 好的提交信息：

```text
feat(001-cards): 新增分类树拖拽调整层级

原来的上下移动按钮只能同层挪动，深层嵌套时要反复点十几次。
改为按住节点拖到目标位置：拖到节点上成为子节点，拖到行的上/下 1/4 区域
成为前/后兄弟；禁止拖入自身子树，避免成环。

Refs: #7
```

```text
chore(repo): 实验室仓库纳入 Git 管理

补齐忽略配置、提交信息模板与 commit-msg / pre-commit 钩子，
让规范和自动提交都能被机器兜底。
```

❌ 会被钩子拦下或需要重写：

```text
update                          # 无类型、无中文、无信息量
fix: fix the bug                # 描述不是中文
feat 新增拖拽                    # 缺少冒号和空格
feat(001-cards):新增拖拽排序      # 冒号后缺空格
feat(001-cards): 新增分类树拖拽调整层级并顺带重构了 store 层把树操作抽出来并统一缩进与命名   # 主题行超过 50 字符，该拆
```

---

## 三、自动提交约定（AI Agent / 脚本）

工作区里跑着 AI Agent 或自动化脚本时，**自动提交是允许的**，但必须满足下面的条件。

### 3.1 允许自动提交的前提

1. 这批改动**已经完成**，是一个可独立描述的原子改动；半成品、跑不通的状态不提交；
2. 该实验的自检已通过（有测试就跑测试，有构建就构建一次）；
3. 提交内容**只包含本次任务的改动**，不夹带调试残留、临时文件、注释掉的代码；
4. 提交信息由自动执行者按第二章规范生成，**不得**用「自动提交」「auto commit」「update by agent」这类占位描述。

### 3.2 粒度与拆分

- 一次自动提交只做一件事；重构与功能改动分开提交；
- 大批量重命名 / 格式化单独成一个 `style` 或 `refactor` 提交，避免淹没真实改动；
- 跨实验的改动拆成多个提交，scope 分别标注；
- 提交前若发现工作区有**不属于本次任务**的改动，先停下来问人，不要顺手带上。

### 3.3 自动提交的硬性禁令

| 禁止 | 原因 |
| --- | --- |
| `git add -A` / `git add .` | 会误纳无关文件；必须显式列路径 |
| `git commit --no-verify` | 绕过格式与忽略校验，等于规范失效 |
| `git push --force` 到 `main` | 改写公共历史，会覆盖他人提交 |
| 提交 `node_modules/`、`dist/`、`.DS_Store`、`.env`、密钥 | 可再生或属于敏感信息（`pre-commit` 会拦） |
| 提交单文件 > 2MB | 撑大仓库；走 Git LFS 或外部存储 |
| 一次提交混合多个实验 | 无法独立回滚，`git log` 失去意义 |

### 3.4 自动提交前的自检脚本（建议）

```bash
git status --short                      # 1. 看清有什么
git diff --staged --stat                # 2. 核对暂存内容
# 3. 跑该实验的自检，例如实验 001：
#    (cd lab/001-knowledge-cards/react-app && npm test)
git log --oneline -1                    # 4. 提交后回看
```

---

## 四、分支、同步与推送

- 主干只有 `main`，默认直接在 `main` 上小步提交（这是个人实验室，不是多人协作仓库）；
- 需要试验性大改动时开短分支：`lab/002-liveskin-preset`、`fix/cards-search`，合完即删；
- 分支命名：`<类型>/<短横线描述>`，全小写；
- 拉取默认 `rebase`（`pull.rebase=true`），保持线性历史；
- **已经推送过的提交不改写**；要撤销就 `git revert`；
- 推送前确认没有把本地实验残留一起推上去：`git push` 后用 `git log origin/main..main` 复查。

### 4.1 发布与标签

各实验**各自发布**，标签带实验前缀，避免与「仓库版本」混淆：

```
<编号>-<名称>-v<主>.<次>.<修>        例如 002-liveskin-v1.0.0
```

发布一次的动作固定为四步：

1. 把该实验 `CHANGELOG.md` 的 `[未发布]` 结为 `[<版本>] · <日期>`，并在最上面新开一个空的 `[未发布]`；
2. 同步改实验自己的 `package.json` 版本号（有的话），并确认**版本在运行时可问**
   —— 否则「线上跑的是哪一版」只能靠 git tag 猜；
3. 更新根 `README.md` 实验表里的状态列；
4. 打**附注标签**（`git tag -a <标签> -m <说明>`）并 `git push origin <标签>`。

标签只增不改：已推送的标签不删除、不重打；打错了就发下一个版本。
仓库根不设版本号 —— 根 `CHANGELOG.md` 用日期里程碑。

---

## 五、忽略配置

忽略规则分两层：

1. **仓库根 `.gitignore`**（版本化，全仓库生效）：

| 规则 | 忽略对象 |
| --- | --- |
| `node_modules/`、`.npm-cache/` | 依赖与包缓存，`npm install` 可再生 |
| `dist/`、`build/`、`*.tsbuildinfo` | 构建产物，`npm run build` 可再生 |
| `.DS_Store`、`Thumbs.db` | 系统垃圾文件 |
| `.idea/`、`.vscode/`、`*.swp` | 编辑器/IDE 本地配置 |
| `*.log`、`*.tmp`、`.env*`、`*.local` | 日志、临时文件与本地环境变量 |

2. **实验自己的 `.gitignore`**（如 `lab/001-knowledge-cards/react-app/.gitignore`）：
   只写该实验特有的产物，**不重复**根目录已有的规则。

刻意入库的例外：`lab/002-liveskin/archive/aero-skin-build/out/`（皮肤构建产物的样本）。
`lab/002-liveskin/test/.home/`（测试用的最小 DSH home）已改为运行时生成，不入库。
它们是实验结论的一部分，不叫 `dist`/`build`，因此不受上面的规则影响——要新增这类例外时，
**必须在 `.gitignore` 里写清原因**，不要靠改名绕过。

---

## 六、变更日志

变更日志按**实验**拆分，各写各的：

| 文件 | 记什么 |
| --- | --- |
| `CHANGELOG.md`（仓库根） | **只记仓库级改动**：目录布局、提交规范与 Git 钩子、忽略配置、根 README 与实验登记 |
| `lab/<编号>-<名称>/CHANGELOG.md` | 该实验的全部改动 |

约定：

- 结构参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)：`[未发布]` 置顶，
  里程碑用 `[名称] · <日期>`；同一里程碑内按 `新增 / 修复 / 变更 / 移除 / 说明` 分组；
- 只记**对使用者或后续开发有意义**的事，写清「为什么」与取舍，不逐条复述 diff
  —— 复查「改了什么」是 `git log` 的职责；
- 数值、路径、行为都要与实际一致。拿不准就别写：日志里一句夸大或过期的话，
  比缺一条记录更难被发现；
- 新增实验时一并创建它的 `CHANGELOG.md`，并在实验 README 里链接过去。

---

## 七、Git 钩子

钩子放在 `.githooks/`（版本化，随仓库分发），通过 `core.hooksPath` 启用：

```bash
git config core.hooksPath .githooks      # 克隆后执行一次，本地配置
```

| 钩子 | 作用 |
| --- | --- |
| `.githooks/commit-msg` | 校验 `type(scope): 中文描述` 格式、中文描述、主题行 ≤ 50 字符；放行 `Merge` / `Revert` / `fixup!` / `squash!` |
| `.githooks/pre-commit` | 拦截依赖目录、构建产物、系统垃圾文件与超过 2MB 的文件 |

- 钩子失败时会打印**期望格式与正确示例**，照着重写即可；
- `git commit --no-verify` 可以跳过，但只用于钩子误判（例如确实要提交一个定制的 `build/` 样本），
  并且要在提交正文里写清为什么跳过；
- 修改钩子本身属于仓库级改动，scope 用 `git`。

---

## 八、FAQ

**Q：实验里我想随手保存一下进度，也要写这么正式吗？**
A：用 `git stash` 或临时分支，别用「wip」污染主干历史。提交信息是给别人（以及三个月后的自己）看的。

**Q：主题行一定要 50 字符以内吗？中文会不会太短？**
A：50 个字符足够说清一件事。说不清说明这次提交包含的事不止一件，按第三章拆分。

**Q：新实验怎么加？**
A：在 `lab/` 下建 `<三位编号>-<英文名>/`，在实验目录写自己的 `README.md`，再在根 `README.md` 的实验清单里登记一行。
